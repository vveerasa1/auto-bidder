{
  "name": "bidman-backend",
  "version": "1.0.0",
  "description": "in this app we going to bid freelance projects automatically using chat gpt and freelancer api",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "SET NODE_ENV=dev&& nodemon server.js",
    "start:local": "SET NODE_ENV=local&& nodemon --inspect server.js",
    "start:prod": "SET NODE_ENV=prod&& node server.js",
    "build:dev": "SET NODE_ENV=dev&& webpack --config webpack.config.js --mode=none",
    "build:local": "SET NODE_ENV=local&& webpack --config webpack.config.js --mode=none",
    "build:prod": "SET NODE_ENV=prod&& webpack --config webpack.config.js --mode=none"
  },
  "keywords": [
    "nodejs",
    "expressjs",
    "mongodb"
  ],
  "author": "pentabay",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "mongoose": "^8.7.0",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "node-fetch": "^3.3.2",
    "nodemailer": "^6.9.15",
    "openai": "^4.68.1",
    "winston": "^3.15.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
